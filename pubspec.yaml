name: dispose_all
description: A Flutter package for reducing boilerplate code when disposing of objects
version: 1.0.0+1
homepage: https://github.com/sergioprot/dispose_all
repository: https://github.com/sergioprot/dispose_all/tree/main
issue_tracker: https://github.com/sergioprot/dispose_all/issues
topics:
  - dispose
  - disposable
  - disposing

environment:
  sdk: '>=3.1.5 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
